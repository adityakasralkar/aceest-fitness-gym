name: ACEest CI/CD Pipeline

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install linting tools
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install flake8==6.1.0 black==23.9.1

      - name: Run Black formatter check
        working-directory: backend
        run: |
          black --check .

      - name: Run Flake8 linter
        working-directory: backend
        run: |
          flake8 .

  build-and-test:
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Pytest
        working-directory: backend
        run: |
          python -m pytest tests/ -v

  docker-build:
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t aceest-backend ./backend

      - name: Run tests inside Docker
        run: |
          docker run --rm \
            -e DATABASE_URL=sqlite:///aceest.db \
            -e FLASK_ENV=testing \
            aceest-backend \
            python -m pytest tests/ -v
